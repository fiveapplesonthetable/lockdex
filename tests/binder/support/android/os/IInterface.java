package android.os;
// Minimal stand-in for the real framework type, so corpus fixtures dex without the
// Android SDK. lockdex keys on the fully-qualified name, which is identical here.
public interface IInterface {}
